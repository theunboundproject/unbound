# Unbound

**Unbound** is an open-source compatibility layer designed to liberate iOS applications from Apple's "walled garden". Our goal is to allow iOS binaries/apps to run natively on Linux, Windows, and Android.
